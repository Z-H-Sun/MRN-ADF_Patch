#!/usr/bin/env ruby
# encoding: ASCII-8Bit
# ruby 2.0.0
require 'io/console'
print "\033]0;ChemDraw 17~26 (Mac) Universal Patcher by Zack\007"

def removeAllSig(filename)
    puts "\e[1;33mProcessing\e[0m #{filename}..."
    f = open(filename, "r+b") # read and write, binary
    if f.read(4) == "\xCA\xFE\xBA\xBE" # Mach-O Fat Binary
        n = f.read(4).unpack('N')[0] # fat_arch_size; big-endian
        o = Array.new(n, 0)
        s = Array.new(n, 0)
        for i in 0...n
            f.seek(8, 1) # skip arch cpu_(sub_)type
            o[i], s[i] = f.read(8).unpack('N2') # arch file_offsset; size
            f.seek(4, 1) # skip alignment
        end
        for i in 0...n
            puts "\e[1;32mMach-O binary found\e[0m @ 0x#{o[i].to_s(16)} (+ 0x#{s[i].to_s(16)})"
            removeSig(f, o[i], s[i])
        end
    else
        removeSig(f, 0, File.size(f))
    end
    f.close
rescue # error
    puts "\e[1;31mError occurred:\e[0m"
    puts $!.inspect
    puts $@.inspect
end

def removeSig(f, offset, size)
    f.seek(offset)
    if f.read(4) != "\xCF\xFA\xED\xFE" # Mach-O binary
        puts "\e[1;33mWarning: Not a Mach-O binary @ 0x#{offset.to_s(16)}. Is this authentic ChemDraw?\e[0m"
        return
    end
    c = f.read(4)
    if c == "\7\0\0\1" # cpu_type==x86_64
      @must_code_sign = false
    elsif c == "\x0C\0\0\1" # cpu_type==ARM64
      @need_code_sign = true
    end
    f.seek(8, 1)
    tmp = f.read(16) # now at: end of mach head
    ncmd, cmdSize = tmp.unpack('L2')
    if cmdSize+32 > size
        puts "\e[1;33mWarning: EOF reached. Is this authentic ChemDraw?\e[0m"
        return
    end
    cmdSizeToMove = cmdSize # a widely adopted trick is to fill the last load cmd with 16 \0's, but that relies on the fact that LC_CODE_SIGNATURE is the last load cmd. This is usually true, but some binaries aren't; to be safe, do this in a safer way
    cmdCodeSigSize = sigOffset = sigSize = 0
    for i in 0...ncmd
        _t, _s, sigOffset, sigSize = f.read(16).unpack('L4')
        cmdSizeToMove -= _s
        if _t == 0x1d # LC_CODE_SIGNATURE
            cmdCodeSigSize = _s # in this case, _s must be 16, but just to be safe
            break
        end
        f.seek(_s - 16, 1)
    end
    if cmdCodeSigSize <= 0
        puts "\e[1;33mWarning: No signature found. Is this authentic ChemDraw?\e[0m"
        return
    end
    puts "\e[1;32mDigital signature load command found\e[0m @ 0x#{(f.pos-cmdCodeSigSize).to_s(16)}"
    if sigOffset+sigSize > size
        puts "\e[1;33mWarning: EOF reached. Is this authentic ChemDraw?\e[0m"
        return
    end
    puts "\e[1;32mDigital signature block found\e[0m @ 0x#{offset.to_s(16)}+#{sigOffset.to_s(16)} (+ 0x#{sigSize.to_s(16)})"
    print "\e[1;32mDo you wish to \e[7mPATCH\e[0m [y/N]? (This cannot be undone) "
    if STDIN.getch.downcase == 'y'
        if cmdSizeToMove > 0
            tmp = f.read(cmdSizeToMove)
            f.seek(-cmdSizeToMove-cmdCodeSigSize, 1)
            f.write(tmp)
        else
            f.seek(-cmdCodeSigSize, 1)
        end
        f.write("\0"*cmdCodeSigSize) # end of load cmd
        f.seek(16+offset)
        f.write([ncmd-1, cmdSize-cmdCodeSigSize].pack('L2'))
        f.seek(sigOffset+offset)
        f.write("\0"*sigSize)
        puts "\e[1;32mSuccessfully removed digital signature.\e[0m"
        return
    end
    puts("\e[1;33mNo operation performed.\e[0m")
rescue # error
    puts "\e[1;31mError occurred:\e[0m"
    puts $!.inspect
    puts $@.inspect
end

def patchAll(filename) # FNEActivationHelperMac::GetActivatedSKUAndProductLevel {return 0}
    puts "\e[1;33mProcessing\e[0m #{filename}..."
    f = open(filename, "r+b") # read and write, binary
    @times = @byte = 0 # number of to-be-patched patterns, processed length

    d = f.read(8)
    if d[0, 4] == "\xCA\xFE\xBA\xBE" # Mach-O Fat Binary
        _, n = d.unpack('N2') # fat_arch_size; big-endian
        o1 = s1 = o2 = s2 = -1
        for i in 0...n
            c = f.read(4) # arch cpu_type
            f.seek(4, 1) # skip arch cpu_sub_type
            o, s = f.read(8).unpack('N2') # arch file_offsset; size
            f.seek(4, 1) # skip alignment
            if c == "\1\0\0\7" and o1 < 0
                o1 = o; s1 = s
            elsif c == "\1\0\0\x0C" and o2 < 0
                o2 = o; s2 = s
            else
                puts "\e[1;33mIgnored: Mach-O binary of unsupported CPU type [0x#{c.unpack('H*')[0]}] found\e[0m @ 0x#{o.to_s(16)} (+ 0x#{s.to_s(16)})"
            end
        end
        if o1 >= 0
          puts "\e[1;32mMach-O binary [x86_64] found\e[0m @ 0x#{o.to_s(16)} (+ 0x#{s.to_s(16)})"
          patchX86(f, o1, o1+s1)
        end
        if o2 >= 0
          puts "\e[1;32mMach-O binary [ARM64] found\e[0m @ 0x#{o.to_s(16)} (+ 0x#{s.to_s(16)})"
          patchARM(f, o2, o2+s2)
        end
    elsif d == "\xCF\xFA\xED\xFE\7\0\0\1" # Mach-O binary and cpu_type==x86_64
        patchX86(f, 0, File.size(f))
    elsif d == "\xCF\xFA\xED\xFE\x0C\0\0\1" # Mach-O binary and cpu_type==ARM64
        patchARM(f, 0, File.size(f))
    else
        puts "\e[1;33mWarning: Not a Mach-O binary or unsupported CPU type. Is this authentic ChemDraw?\e[0m"
    end
    f.close
    if @times.zero?
        # not applicable
        puts "\e[1;33mNo pattern found or canceled by user.\e[0m"
    else
        puts "\e[1;32mPatched/Restored #{@times} place(s) in this file.\e[0m"
    end
rescue # error
    puts "\e[1;31mError occurred:\e[0m"
    puts $!.inspect
    puts $@.inspect
end

def patchX86(f, offset, offset_max)
    puts "\e[1;33mProcessing x86_64 architecture\e[0m..."
    f.seek(offset)
    loop do
        maxlen = offset_max - f.pos
        b = f.gets("\xC3", maxlen) # read until met with 0xC3 (RET); do not read further than `offset_max`
        break if b.nil? # EOF
        len = b.size # read length
        p = f.pos
        byte = (p / 104857.6).to_i
        print "\r\e[1;33mProcessed %.1f MB.\e[0m " % (byte/10.0) if byte != @byte
        @byte = byte
        break if len == maxlen # EOF

        next if len < 30
        if b[-26, 4] == "\x48\x89\x1f\xe8" and # mov [rdi], rbx; call ...
           b[-12].ord < 0x30 # add rsp, 28h [There are multiple functions that matches with the above pattern, so need to restrict more; this function only needs to add 28 bytes to balance the stack, while others requires more
        # Ver 25: 48 8D 7D C8 48 89 1F E8 ..{4} 44 89 F0 48 83 C4 28 5B 41 5C 41 5D 41 5E 41 5F 5D C3
            p -= 26
            case b[-18, 3]
            when "\x44\x89\xf0" # to-patch
                puts "\e[1;32m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, MOV EAX, R14D -> XOR EAX, EAX: 48 89 C6 1F E8 ..{4} \e[7m44 89 F0\e[0m ..{14} C3 => \e[7m31 C0 90\e[0m"
                print "\e[1;32mDo you wish to \e[7mPATCH\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-18, 1); f.write("\x31\xc0\x90"); f.seek(15, 1)
            when "\x31\xc0\x90" # patched
                puts "\e[1;34m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, XOR EAX, EAX -> MOV EAX, R14D: 48 89 C6 1F E8 ..{4} \e[7m31 C0 90\e[0m ..{14} C3 => \e[7m44 89 F0\e[0m"
                print "\e[1;34mDo you wish to \e[7mRESTORE\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-18, 1); f.write("\x44\x89\xf0"); f.seek(15, 1)
            else next
            end
        elsif b[-30, 3] == "\x41\x89\xc6"
        # Ver 18..23: 41 89 C6 48 8D 7D B8 ..{5} 44 89 F0 48 83 C4 28 5B 41 5C 41 5D 41 5E 41 5F 5D C3
            p -= 30
            case b[-18, 3]
            when "\x44\x89\xf0" # to-patch
                puts "\e[1;32m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, MOV EAX, R14D -> XOR EAX, EAX: 41 89 C6 ..{9} \e[7m44 89 F0\e[0m ..{14} C3 => \e[7m31 C0 90\e[0m"
                print "\e[1;32mDo you wish to \e[7mPATCH\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-18, 1); f.write("\x31\xc0\x90"); f.seek(15, 1)
            when "\x31\xc0\x90" # patched
                puts "\e[1;34m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, XOR EAX, EAX -> MOV EAX, R14D: 41 89 C6 ..{9} \e[7m31 C0 90\e[0m ..{14} C3 => \e[7m44 89 F0\e[0m"
                print "\e[1;34mDo you wish to \e[7mRESTORE\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-18, 1); f.write("\x44\x89\xf0"); f.seek(15, 1)
            else next
            end
        elsif b[-28, 3] == "\x45\x31\xe4"
        # Ver 17: 45 31 E4 48 8D 7D B0 ..{5} 44 88 E0 48 83 C4 40 5B 41 5C 41 5E 41 5F 5D C3
            p -= 28
            case b[-16, 3]
            when "\x44\x88\xe0" # to-patch
                puts "\e[1;32m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, MOV AL, R12B -> MOV AL, 0x01: 45 31 E4 ..{9} \e[7m44 88 E0\e[0m ..{12} C3 => \e[7mB0 01 90\e[0m"
                print "\e[1;32mDo you wish to \e[7mPATCH\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-16, 1); f.write("\xb0\x01\x90"); f.seek(13, 1)
            when "\xb0\x01\x90" # patched
                puts "\e[1;34m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, MOV AL, 0x01 -> MOV AL, R12B: 45 31 E4 ..{9} \e[7mB0 01 90\e[0m ..{12} C3 => \e[7m44 88 E0\e[0m"
                print "\e[1;34mDo you wish to \e[7mRESTORE\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-16, 1); f.write("\x44\x88\xe0"); f.seek(13, 1)
            else next
            end
        else next # not a to-be-patched pattern; roll back
        end
        @times += 1
        puts "\e[1;32mOperation performed.\e[0m"
        return # there are multiple patterns; however, only the first one is what we desire
    end
    puts "\e[1;33mNo operation performed.\e[0m"
rescue # error
    puts "\e[1;31mError occurred:\e[0m"
    puts $!.inspect
    puts $@.inspect
end

def patchARM(f, offset, offset_max)
    puts "\e[1;33mProcessing ARM64 architecture\e[0m..."
    f.seek(offset)
    loop do
        maxlen = offset_max - f.pos
        b = f.gets("\xC0\x03\x5F\xD6", maxlen) # read until met with C0 03 5F D6 (RET); do not read further than `offset_max`
        break if b.nil? # EOF
        len = b.size # read length
        p = f.pos
        byte = (p / 104857.6).to_i
        print "\r\e[1;33mProcessed %.1f MB.\e[0m " % (byte/10.0) if byte != @byte
        @byte = byte
        break if len == maxlen # EOF

        next if len < 45
        if b[-45, 5] == "\x97\xF3\x03\x00\xAA" # BL ...; MOV X19, X0
        # Ver 25:  BL <xxxToLevCode>; MOV X19, X0; ...; BL <destroy_vector>; MOV X0, X19; LDP X29, X30, ...; ADD SP, SP, #0x70; RET
            p -= 45
            case b[-28, 4]
            when "\xE0\x03\x13\xAA" # to-patch
                puts "\e[1;32m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, MOV X0, X19 -> MOV X0, #0: 97 F3 03 00 AA ..{12} \e[7mE0 03 13 AA\e[0m ..{20} C0 03 5F D6 => \e[7m00 00 80 D2\e[0m"
                print "\e[1;32mDo you wish to \e[7mPATCH\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-28, 1); f.write("\x00\x00\x80\xD2"); f.seek(24, 1)
            when "\x00\x00\x80\xD2" # patched
                puts "\e[1;34m\rFound pattern\e[0m @ 0x#{p.to_s(16)}, MOV X0, #0 -> MOV X0, X19: 97 F3 03 00 AA ..{12} \e[7m00 00 80 D2\e[0m ..{20} C0 03 5F D6 => \e[7mE0 03 13 AA\e[0m"
                print "\e[1;34mDo you wish to \e[7mRESTORE\e[0m [y/N]? "
                break if STDIN.getch.downcase == 'n' # canceled by user
                f.seek(-28, 1); f.write("\xE0\x03\x13\xAA"); f.seek(24, 1)
            else next
            end
        else next # not a to-be-patched pattern; roll back
        end
        @times += 1
        puts "\e[1;32mOperation performed.\e[0m"
        return # there are multiple patterns; however, only the first one is what we desire
    end
    puts "\e[1;33mNo operation performed.\e[0m"
rescue # error
    puts "\e[1;31mError occurred:\e[0m"
    puts $!.inspect
    puts $@.inspect
end

# Main
@vers = 0 # total number of versions of ChemDraw
Dir.entries('/Applications').each do |i|
    next unless i.include?('ChemDraw')
    if i == 'ChemDraw.app' # for versions >= 26, the version won't be in the app name
      ver = 0
      vf = File.join('/Applications', i, 'Contents/Info.plist') # so, instead read the version from the package's meta data
      if File.exist?(vf)
        open(vf) do |f|
          f.each_line do |l|
            if l.include?('CFBundleShortVersionString')
              l = f.gets('<key>')
              break if l.nil?
              vf = l[/\d+\.?\d*/]
              ver = vf.to_f
              break
            end
          end
        end
      end
      name = ver.zero? ? 'ChemDraw (unknown version)' : 'ChemDraw ' + vf
    else
      name = i[0...-4]
      ver = i[/\d+\.?\d*/].to_f
    end
    puts; print "\e[7m"; puts '-'*50; puts name.ljust(50); puts '-'*50; print "\e[0m"
    s = File.join('/Applications', i, 'Contents/MacOS/ChemDraw')
    if ver < 17
        puts "\e[1;31mNot applicable (Version number #{ver} < 17)\e[0m"; next
    elsif ver < 18
        f = s
    else
        f = File.join('/Applications', i, 'Contents/Frameworks/ChemDrawBase.framework/Versions/A/ChemDrawBase')
    end
    if File.exist?(f) and File.exist?(s)
        @need_code_sign = false
        @must_code_sign = true
        removeAllSig(s); patchAll(f); @vers += 1
        if @need_code_sign
            puts "\n\e[1;31mNOTE: Further actions may be necessary to complete the patch. Please read below carefully.\e[0m\n\nStarting from ChemDraw 25.0, support on ARM64 machines (i.e., Macs with Apple Silicon processors) have been implemented; however, MacOS > 10 requires a valid digital signature for an ARM64-architected app to run."
            if @must_code_sign
                puts "\e[1;33mTherefore, it is necessary for ChemDraw to be signed.\e[0m Press any key to proceed to complete this process by the `codesign` tool."
                STDIN.getch
            else
                puts "1. If you are using a Mac with Intel processors, you are all set! Feel free to start using ChemDraw now.\n2. Otherwise, you can open ChemDraw with Rosetta; then ChemDraw will also run properly.\n3. If you cannot run Rosetta on a ARM64 Mac, ChemDraw must be signed in order to run. \e[1;33mUse `codesign` tool to complete this process?\e[0m [y/N]?\e[1;31m"
                next if STDIN.getch.downcase != 'y'
            end
            system("codesign --force --deep -s - \"#{s}\"
if [ \"$?\" -eq 0 ]; then echo \"\e[1;32mSuccessfully signed.\e[0m\"; else echo \"Failed to sign. ChemDraw may not run properly.\e[0m\"; fi")
        end
    else
        puts "\e[1;31mNot applicable (File not found)\e[0m"
    end
end
puts
if @vers.zero?
    puts "\e[1;31mNo valid version of ChemDraw detected\e[0m"
else
    puts "\e[1;32mIn total, #{@vers} version(s) of Chemdraw processed\e[0m"
end
